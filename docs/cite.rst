Use of cite shortcut notation
-----------------------------

You can also use the ``@identifier`` notation. It creates a link with
default text and default caption. If ``cite-shortcut: False`` is in the
meta-data block for a given category, this feature is disabled.

